/**
 * Correct answer for multiple string answers (e.g. order, match, or set of correct items).
 */
export interface StringArrayCorrect {
  /** Array of correct strings (order matters if relevant) */
  values: string[];
}
